def find_minimum_arborescence_frank(D: nx.DiGraph, r0: str = "r0"):
    """
    Encontra uma r-arborescência mínima pelo método de Frank em duas fases.
    """
    D_zero, A_zero = phase1_find_minimum_arborescence(D, r0)
    T = phase2_build_arborescence(D, r0, A_zero)
    return T
